@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>

<h1>Bowler's Contact Info</h1>

<ul>
    @foreach (var b in Model)
    {
        <li>@b.BowlerID - @b.BowlerFirstName @b.BowlerLastName  </li>
    }
</ul>
